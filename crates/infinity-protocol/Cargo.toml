[package]
name = "infinity-protocol"
publish = true
version = "0.1.0"
documentation = "https://docs.rs/infinity-protocol/"
description = "Wire protocol shared between the Infinity daemon and its clients."
edition = "2024"
repository = { workspace = true }
license = "Apache-2.0"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
dirs = "6"
rap-protocol = { path = "../rap-protocol", version = "^0.1.0" }
tokio-util = { version = "0.7", features = ["codec"] }

[lints]
workspace = true
